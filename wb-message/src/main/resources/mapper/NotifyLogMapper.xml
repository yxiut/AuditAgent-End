<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wb.audit.message.mapper.NotifyLogMapper">

    <select id="selectById" parameterType="long" resultType="com.wb.audit.message.entity.NotifyLog">
        SELECT id, task_id, template_id, target_user_id, wecom_userid, notify_type, content, msg_id, channel, status, send_result, sent_at, create_time, update_time FROM wb_notify_log WHERE id = #{id}
    </select>

    <select id="selectAll" resultType="com.wb.audit.message.entity.NotifyLog">
        SELECT id, task_id, template_id, target_user_id, wecom_userid, notify_type, content, msg_id, channel, status, send_result, sent_at, create_time, update_time FROM wb_notify_log
    </select>

    <insert id="insert" parameterType="com.wb.audit.message.entity.NotifyLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO wb_notify_log (task_id, template_id, target_user_id, wecom_userid, notify_type, content, msg_id, channel, status, send_result, sent_at, create_time, update_time)
        VALUES (#{taskId}, #{templateId}, #{targetUserId}, #{wecomUserid}, #{notifyType}, #{content}, #{msgId}, #{channel}, #{status}, #{sendResult}, #{sentAt}, #{createTime}, #{updateTime})
    </insert>

    <delete id="deleteById" parameterType="long">
        DELETE FROM wb_notify_log WHERE id = #{id}
    </delete>

</mapper>
